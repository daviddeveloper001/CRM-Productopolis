models:
  PaymentMethod:
    method: string:400
  
  Department:
    name: string:400
    relationships:
      hasMany: City
  
  City:
    name: string:400
    relationships:
      belongsTo: Department

  ReturnAlert:
    status: enum:Top,Medium,Low

  Shop:
    name: string:400

seeders: PaymentMethod, Department, City, ReturnAlert, Shop